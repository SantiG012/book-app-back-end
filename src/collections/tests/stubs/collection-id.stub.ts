import { CollectionIdDto } from "../../dtos/index";

export const collectionIdStub = ():CollectionIdDto =>{
    const collectionId:CollectionIdDto = {
        collectionId:'collectionId'
    }

    return collectionId;
}